# Artifact policy

Artifact existence is an independent promotion gate when artifacts are required. An empty artifact collection is `NOT_FOUND` at the artifact gate, not evidence of success. This prevents a workflow/check/job success from masking a missing required deliverable.
