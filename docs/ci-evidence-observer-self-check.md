# Self-check status

The observer implementation and deterministic unit tests are committed on `factory/ci-evidence-observer-20260904`. Authoritative GitHub Actions execution must still be used to validate these commits. The observer must never report its own unexecuted tests as CI success.
