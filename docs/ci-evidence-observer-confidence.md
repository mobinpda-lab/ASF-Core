# Evidence confidence

`HIGH` is reserved for terminal success observations with exact repository/SHA correlation. Non-success visibility is `LOW` and always blocks promotion. Confidence is descriptive; it never overrides a gate status. The Production Orchestrator remains the only promotion authority.
