# Promotion decision protocol

For each required gate, record Gate, Status, Evidence, Confidence, Decision. `ALLOW` is valid only for `SUCCESS` with exact repository/SHA correlation and satisfied required evidence. All unknown, missing, inaccessible, pending, or failed evidence blocks promotion. Production Orchestrator is the sole promotion authority.
